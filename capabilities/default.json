{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability — core APIs + this app's custom commands",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
